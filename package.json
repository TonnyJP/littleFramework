{
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "json-server -w db.json",
    "start": "concurrently npm:start:*"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  }
}
